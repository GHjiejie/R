# Redis 学习项目约定

## 适用范围

本文件适用于整个项目目录及其所有子目录。除非用户明确提出其他要求，否则所有新增和修改内容都应遵守以下约定。

## 项目定位

本项目用于学习和实践 Redis，包括：

- Redis 基础概念和常用命令
- String、List、Hash、Set、Sorted Set 等数据结构
- 持久化、事务、发布订阅和 Lua 脚本
- 缓存设计、过期策略和常见问题
- Redis 实战案例和性能相关实践

## 学习总结

所有 Redis 概念总结必须保存到 `summarize/` 目录中。

每次用户要求总结一个新概念时：

1. 创建新的 Markdown 文件，不覆盖已有总结。
2. 文件名必须包含时间戳，格式为：

   ```text
   YYYY-MM-DD-HHmmss-概念名称.md
   ```

3. 时间统一使用 `Asia/Shanghai` 时区。
4. 概念名称应使用简短、易读且适合文件名的形式，避免使用 `/`、空格等特殊字符。
5. 文件正文开头必须记录时间，例如：

   ```text
   记录时间：2026-09-11 17:30:00 (Asia/Shanghai)
   ```

总结内容建议包含以下部分：

- 概念简介
- 核心机制
- 常用命令或 API
- 使用示例
- 运行结果或现象
- 常见问题和注意事项
- 与相关 Redis 概念的区别或联系

如果需要补充已有概念，应创建新的带时间戳的总结文件，并在正文中关联旧记录，不直接覆盖旧文件。

## Demo 目录

项目中用于演示 Redis 某个独立知识点的目录均视为 demo 目录，例如 `demo/`、`demo-string/`、`demo-pubsub/` 等。

每一个 demo 目录都必须包含同级的 `README.md`。新增或修改 demo 时，必须同步检查和更新该文件。

每个 demo 的 `README.md` 至少应包含：

- Demo 目的
- 涉及的 Redis 知识点
- 目录或关键文件说明
- 运行前置条件
- 启动或运行方式
- 预期输出或可观察现象
- 清理方式（适用时）
- 常见问题（适用时）

Demo 应遵循以下原则：

- 一个 demo 尽量只演示一个主题。
- 示例代码、配置文件和测试数据放在对应 demo 目录中。
- 不要把不同主题的代码混放在同一个 demo 中。
- 不要提交真实密码、Token 或其他敏感信息。
- 配置示例应优先使用 `.env.example` 或明确的示例配置。

## 文档规范

- 文档默认使用中文。
- Redis 命令、API、配置项、代码和专有名词保留原文。
- Markdown 应保持结构清晰，代码示例必须使用代码块。
- 修改代码后，如果对应 demo 的行为、运行方式或输出发生变化，必须同步更新 README。
- 新增总结或 demo 前，先检查现有目录和文件，避免重复创建或覆盖已有内容。
- 文档中需要绘图时（如流程图、时序图、架构图等），必须使用 mermaid 代码块（```mermaid），不要使用图片文件或纯 ASCII 图。

## 完成检查

完成任务后，应至少检查：

- 新增总结是否位于 `summarize/` 目录。
- 总结文件名和正文是否包含时间戳。
- 每个 demo 目录是否包含 `README.md`。
- README 中的运行说明是否与实际代码一致。
- 是否误提交了密码、Token 或其他敏感信息。
- 是否保留了已有学习记录和示例。